import SwiftUI
import PocketCastsServer

struct SubscriptionBadge2024: View {
    init(subscriptionTier: SubscriptionTier) {
        _ = subscriptionTier
    }

    var body: some View {
        EmptyView()
    }
}

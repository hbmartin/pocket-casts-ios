{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -q '\\.swift$'; then echo \"Running static checks after $FILE_PATH\"; if make static_checks; then echo \"Static checks completed\"; else echo \"Static checks failed\"; exit 1; fi; fi"
          }
        ]
      }
    ]
  }
}

{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -q '\\.swift$'; then echo \"Running lint after $FILE_PATH\"; if make lint; then echo \"Lint completed\"; else echo \"Lint failed\"; exit 1; fi; fi"
          }
        ]
      }
    ]
  }
}

{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -q '\\.swift$'; then echo \"Running static checks after $FILE_PATH\"; make static_checks && echo \"Static checks completed\" || echo \"Static checks failed\"; fi"
          }
        ]
      }
    ]
  }
}

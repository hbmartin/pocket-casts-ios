min_version = "2025.1.0"

[tools]
# Keep in sync with .ruby-version (still read by rbenv users and editor tooling).
ruby = "3.4.9"
# uv powers the pipx backend that installs semgrep (it provisions its own Python).
uv = "0.11.20"
semgrep = "1.151.0"

# -----------------------------------------------------------------------------
# Setup
# -----------------------------------------------------------------------------
[tasks."setup:deps"]
description = "Install Ruby gem dependencies (fastlane et al.)"
run = "bundle install"

[tasks."setup:credentials"]
description = "Generate an empty LocalApiCredentials.swift so the app builds (external contributors)"
alias = "external-contributor"
run = "./scripts/generate-placeholder-credentials.sh"

# -----------------------------------------------------------------------------
# Lint / format (SwiftLint via the BuildTools Swift Package plugin).
# Explicit --config prevents SwiftLint from picking up nested configs in
# BuildTools/.build/checkouts/ (e.g., SwiftGenPlugin's .swiftlint.yml).
# -----------------------------------------------------------------------------
[tasks.lint]
description = "Lint the codebase with SwiftLint"
run = "./scripts/run-buildtools-plugin.sh swiftlint lint --working-directory .. --config .swiftlint.yml --quiet"

[tasks."lint:lenient"]
description = "Lint with SwiftLint, treating errors as warnings"
run = "./scripts/run-buildtools-plugin.sh swiftlint lint --working-directory .. --config .swiftlint.yml --quiet --lenient"

[tasks.format]
description = "Lint and autocorrect SwiftLint violations"
run = "./scripts/run-buildtools-plugin.sh swiftlint lint --working-directory .. --config .swiftlint.yml --quiet --autocorrect"

# -----------------------------------------------------------------------------
# Code generation
# -----------------------------------------------------------------------------
[tasks."generate:code"]
description = "Regenerate SwiftGen-managed resources (swiftgen.yml)"
run = "./scripts/run-buildtools-plugin.sh generate-code-for-resources --config ../swiftgen.yml"

[tasks."generate:colors"]
description = "Generate colors and themes from scripts/themes/theme.csv"
run = "ruby scripts/themes/generate_themes.rb scripts/themes/theme.csv"

[tasks."generate:proto"]
description = "Regenerate protobuf Swift files. Usage: mise run generate:proto /path/to/pocketcasts-api/api/modules/protobuf/src/main/proto"
run = "./scripts/update_proto.sh"

[tasks."generate:filesync-proto"]
description = "Regenerate the PocketCastsFileSync protobuf Swift files from the vendored schemas in Modules/Sources/PocketCastsFileSync/Proto"
run = "./scripts/generate-filesync-proto.sh"

# -----------------------------------------------------------------------------
# Build / test
# -----------------------------------------------------------------------------
[tasks.build]
description = "Build the pocketcasts scheme (Debug) for the generic simulator"
run = '''
xcodebuild -project podcasts.xcodeproj \
    -scheme pocketcasts \
    -configuration Debug \
    -destination 'generic/platform=iOS Simulator' \
    build
'''

[tasks."build:staging"]
description = "Build the 'Pocket Casts Staging' scheme (StagingDebug)"
run = '''
xcodebuild -project podcasts.xcodeproj \
    -scheme "Pocket Casts Staging" \
    -configuration StagingDebug \
    -destination 'generic/platform=iOS Simulator' \
    build
'''

[tasks.clean]
description = "Clean the build artifacts"
run = '''
xcodebuild -project podcasts.xcodeproj \
    -scheme pocketcasts \
    -configuration Debug \
    clean
'''

[tasks.test]
description = "Run unit tests (pocketcasts scheme). Override ONLY_TESTING / SIMULATOR_OS / SIMULATOR_NAME via env"
run = '''
DESTINATION="$(SIMULATOR_OS="${SIMULATOR_OS:-26.5}" ruby scripts/ci/select-ios-simulator.rb)"
CRASH_STATE="$(mktemp)"
./scripts/ci/check-crash-reports.sh snapshot "$CRASH_STATE"
TEST_STATUS=0
xcodebuild test -project podcasts.xcodeproj \
    -scheme pocketcasts \
    -only-testing:"${ONLY_TESTING:-PocketCastsTests}" \
    -destination "$DESTINATION" || TEST_STATUS=$?
./scripts/ci/check-crash-reports.sh check "$CRASH_STATE" || exit 1
exit $TEST_STATUS
'''

[tasks."test:staging"]
description = "Run unit tests (Pocket Casts Staging scheme). Override ONLY_TESTING / SIMULATOR_OS / SIMULATOR_NAME via env"
run = '''
DESTINATION="$(SIMULATOR_OS="${SIMULATOR_OS:-26.5}" ruby scripts/ci/select-ios-simulator.rb)"
CRASH_STATE="$(mktemp)"
./scripts/ci/check-crash-reports.sh snapshot "$CRASH_STATE"
TEST_STATUS=0
xcodebuild test -project podcasts.xcodeproj \
    -scheme "Pocket Casts Staging" \
    -only-testing:"${ONLY_TESTING:-PocketCastsTests}" \
    -destination "$DESTINATION" || TEST_STATUS=$?
./scripts/ci/check-crash-reports.sh check "$CRASH_STATE" || exit 1
exit $TEST_STATUS
'''

[tasks."test:tsan"]
description = "Run unit tests under Thread Sanitizer (Pocket Casts Staging scheme; ONLY_TESTING/SIMULATOR_OS overridable)"
run = '''
DESTINATION="$(SIMULATOR_OS="${SIMULATOR_OS:-26.5}" ruby scripts/ci/select-ios-simulator.rb)"
CRASH_STATE="$(mktemp)"
./scripts/ci/check-crash-reports.sh snapshot "$CRASH_STATE"
TEST_STATUS=0
xcodebuild test -project podcasts.xcodeproj \
    -scheme "Pocket Casts Staging" \
    -only-testing:"${ONLY_TESTING:-PocketCastsTests}" \
    -enableThreadSanitizer YES \
    -destination "$DESTINATION" || TEST_STATUS=$?
./scripts/ci/check-crash-reports.sh check "$CRASH_STATE" || exit 1
exit $TEST_STATUS
'''

[tasks."test:smoke-ui"]
description = "Run the PocketCastsUITests launch/navigation smoke suite on the simulator (SIMULATOR_OS to override)"
run = '''
DESTINATION="$(SIMULATOR_OS="${SIMULATOR_OS:-26.5}" ruby scripts/ci/select-ios-simulator.rb)"
CRASH_STATE="$(mktemp)"
./scripts/ci/check-crash-reports.sh snapshot "$CRASH_STATE"
TEST_STATUS=0
xcodebuild test -project podcasts.xcodeproj \
    -scheme "Pocket Casts Staging" \
    -testPlan SmokeUITests \
    -destination "$DESTINATION" || TEST_STATUS=$?
./scripts/ci/check-crash-reports.sh check "$CRASH_STATE" || exit 1
exit $TEST_STATUS
'''

[tasks."smoke:launch"]
description = "Verify the staging app launches on the simulator and stays alive (SMOKE_APP_PATH to skip the build; SMOKE_SETTLE_SECONDS, SMOKE_SCREENSHOT_PATH)"
run = '''
APP_PATH="${SMOKE_APP_PATH:-}"
if [ -z "$APP_PATH" ]; then
    DERIVED_DATA="${SMOKE_DERIVED_DATA_PATH:-/tmp/pocketcasts-smoke-deriveddata}"
    xcodebuild -quiet -project podcasts.xcodeproj \
        -scheme "Pocket Casts Staging" \
        -configuration StagingDebug \
        -destination 'generic/platform=iOS Simulator' \
        -derivedDataPath "$DERIVED_DATA" \
        CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build
    APP_PATH="$DERIVED_DATA/Build/Products/StagingDebug-iphonesimulator/podcasts.app"
fi
./scripts/ci/smoke-launch.sh "$APP_PATH" "" "${SMOKE_SETTLE_SECONDS:-30}" "${SMOKE_SCREENSHOT_PATH:-}"
'''

[tasks."test:scripts"]
description = "Run tests for the repo's helper scripts (credentials generation, simulator selection, SwiftLint version sync, concurrency/count/coverage ratchets)"
alias = "test:credentials-script"
run = [
    "ruby scripts/tests/generate_credentials_test.rb",
    "ruby scripts/tests/select_ios_simulator_test.rb",
    "ruby scripts/tests/check_snapshot_coverage_test.rb",
    "ruby scripts/tests/swiftlint_version_sync_test.rb",
    "ruby scripts/tests/sync_swiftlint_version_test.rb",
    "./scripts/tests/check_concurrency_warnings_test.sh",
    "./scripts/tests/check_count_ratchets_test.sh",
    "./scripts/tests/check_coverage_floor_test.sh",
]

[tasks."sync:swiftlint-version"]
description = "Sync BuildTools SwiftLintPlugins pin + lock to .swiftlint.yml's swiftlint_version"
run = [
    "ruby scripts/sync_swiftlint_version.rb",
    "swift package --package-path BuildTools resolve",
]

# -----------------------------------------------------------------------------
# Semgrep
# -----------------------------------------------------------------------------
[tasks."semgrep:security"]
description = "Run akabe1 Swift/iOS Semgrep security rules (SEMGREP_SWIFT_ERROR=0 for report-only)"
run = '''
if [ "${SEMGREP_SWIFT_ERROR:-1}" = "1" ]; then ERROR_FLAG="--error"; else ERROR_FLAG=""; fi
semgrep scan --config semgrep/swift-security.yml --include "*.swift" --include "**/Package.swift" --include "**/Package.resolved" --include "**/.github/workflows/*.yml" --include "**/scripts/ci/*.sh" --include "**/scripts/build-phases/*.sh" --include "**/semgrep/*.yml" --include "**/semgrep/*.yaml" --include "mise.toml" --include "**/mise.toml" --exclude "semgrep/tests/**" --metrics off --timeout 0 --disable-version-check $ERROR_FLAG
'''

[tasks."semgrep:pocket-casts"]
description = "Run Pocket Casts custom Semgrep rules (SEMGREP_POCKET_CASTS_ERROR=0 for report-only)"
run = '''
if [ "${SEMGREP_POCKET_CASTS_ERROR:-1}" = "1" ]; then ERROR_FLAG="--error"; else ERROR_FLAG=""; fi
semgrep scan --config semgrep/pocket-casts.yml --config semgrep/swift-datamodel-sql.yml --include "*.swift" --exclude "semgrep/tests/**" --metrics off --timeout 0 --disable-version-check $ERROR_FLAG
'''

[tasks."semgrep:tests"]
description = "Run Semgrep rule tests (fixture list lives in scripts/ci/run-semgrep-tests.sh, shared with CI)"
run = "./scripts/ci/run-semgrep-tests.sh"

# -----------------------------------------------------------------------------
# Static analysis
# -----------------------------------------------------------------------------
[tasks."check:analyzer"]
description = "Run the Xcode static analyzer for the staging app (XCODE_ANALYZE_* env overrides)"
run = '''
DERIVED_DATA="${XCODE_ANALYZE_DERIVED_DATA_PATH:-/tmp/pocketcasts-analyze-deriveddata}"
if [ -n "$DERIVED_DATA" ]; then rm -rf "${DERIVED_DATA}/SDKStatCaches.noindex"; fi
xcodebuild -quiet analyze -project podcasts.xcodeproj \
    -scheme "${XCODE_ANALYZE_SCHEME:-Pocket Casts Staging}" \
    -configuration "${XCODE_ANALYZE_CONFIGURATION:-StagingDebug}" \
    -destination "${XCODE_ANALYZE_DESTINATION:-generic/platform=iOS Simulator}" \
    -derivedDataPath "$DERIVED_DATA" \
    CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
'''

[tasks."check:concurrency"]
description = "Build staging (app target only) and fail on strict-concurrency warnings not in scripts/ci/concurrency-baseline.txt. CI runs the same check on the test log, so test/module baseline entries surface there, not locally."
run = '''
LOG="${CONCURRENCY_WARNINGS_LOG:-/tmp/pocketcasts-concurrency-check.log}"
xcodebuild -project podcasts.xcodeproj \
    -scheme "Pocket Casts Staging" \
    -configuration StagingDebug \
    -destination 'generic/platform=iOS Simulator' \
    CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO \
    build > "$LOG" 2>&1
./scripts/ci/check-concurrency-warnings.sh "$LOG"
'''

[tasks."concurrency:baseline"]
description = "Regenerate scripts/ci/concurrency-baseline.txt from a clean staging build (deletion-only ratchet; see MODERNIZATION.md)"
run = '''
set -eu
DERIVED_DATA="$(mktemp -d /tmp/pocketcasts-concurrency-baseline-dd.XXXXXX)"
LOG="${CONCURRENCY_BASELINE_LOG:-/tmp/pocketcasts-concurrency-baseline.log}"
trap 'rm -rf "${DERIVED_DATA:?}"' EXIT
# build-for-testing so test targets are covered too: CI runs the ratchet on the test log.
xcodebuild -project podcasts.xcodeproj \
    -scheme "Pocket Casts Staging" \
    -configuration StagingDebug \
    -destination 'generic/platform=iOS Simulator' \
    -derivedDataPath "$DERIVED_DATA" \
    CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO \
    build-for-testing > "$LOG" 2>&1
./scripts/ci/check-concurrency-warnings.sh --print-normalized "$LOG" > scripts/ci/concurrency-baseline.txt
echo "Wrote $(wc -l < scripts/ci/concurrency-baseline.txt | tr -d ' ') entries to scripts/ci/concurrency-baseline.txt"
'''

[tasks."check:ratchets"]
description = "Fail on new '@unchecked Sendable' or non-test 'as!' usages above the committed per-file baselines (scripts/ci/ratchet-*.txt)"
run = "./scripts/ci/check-count-ratchets.sh"

[tasks."ratchets:baseline"]
description = "Regenerate scripts/ci/ratchet-*.txt count-ratchet baselines (deletion-only ratchet; run after removing usages)"
run = "./scripts/ci/check-count-ratchets.sh --update-baselines"

[tasks."check:static"]
description = "Run SwiftLint, helper script tests, Semgrep, count ratchets, and the Xcode static analyzer"
alias = "static-checks"
depends = [
    "lint",
    "test:scripts",
    "semgrep:tests",
    "semgrep:security",
    "semgrep:pocket-casts",
    "check:ratchets",
    "check:analyzer",
]

# -----------------------------------------------------------------------------
# Misc
# -----------------------------------------------------------------------------
[tasks."stats:swift-percentage"]
description = "Report Swift vs Obj-C percentage in the project"
run = "./scripts/count.rb"
